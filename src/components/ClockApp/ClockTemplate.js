import './../../styles/ClockApp/ClockTemplate.scss';

function ClockTemplate({children}) {
    return (
        <div className='ClockTemplate'>
            {children}
        </div>
    );
}

export default ClockTemplate;