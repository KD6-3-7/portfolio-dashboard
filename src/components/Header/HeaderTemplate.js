import './../../styles/Header/HeaderTemplate.scss';

function HeaderTemplate({children}) {
    return (
        <div className='HeaderTemplate'>
            {children}
        </div>
    );
}

export default HeaderTemplate;