import './../../styles/PlayerApp/PlayerTemplate.scss';

function PlayerTemplate({children}) {
    return (
        <div className='PlayerTemplate'>
            {children}
        </div>
    );
}

export default PlayerTemplate;